#!/usr/bin/env bash

# Copy .aider-env.sample to .aider-env and modify it.
# Load settings in the following order:
#   1. Default settings (.aider-env.default)
#   2. User settings (.aider-env)
# .aider-env overrides .aider-env.default settings.

DISCORD_WEBHOOK="https://discord.com/api/webhooks/..."
INIT_CRON_JOBS="@reboot ${HOME}/bin/aider/aider init startup
#52 8 */3 * * ${HOME}/bin/aider/aider rclone backup general"
INIT_TOTAL_FOLDERS=983500
INIT_STARTUP="ls ${HOME}
ls /tmp"
LOG_DIR="${HOME}/logs"
LOG_FILE="${LOG_DIR}/${NAME%.*}.log"
LOG_LEVEL="DEBUG"
TAILOG_NGINX_LOGS=(${LOG_DIR}/nginx/*.log)
TAILOG_NGINX_RES=(
    "${TAILOG_NGINX_RES[@]}"
    "sed pattern here"
)